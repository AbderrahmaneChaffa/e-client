{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "start:all": "node -e \"console.log('E-Client EPO démarré. Serveur: http://127.0.0.1:8000 | Queue: imports,default | Vite: hot reload')\" && concurrently --kill-others-on-fail --names \"serve,vite,queue\" --prefix \"[{name}]\" --prefix-colors \"blue,magenta,yellow\" \"php artisan serve --port=8000\" \"npm run dev\" \"php artisan queue:work database --queue=imports,default --timeout=7200 --memory=1024 --tries=1 --sleep=1\"",
        "start:all:prod": "node -e \"console.log('E-Client EPO démarré. Serveur: http://127.0.0.1:8000 | Queue: imports,default | Assets: build')\" && concurrently --kill-others-on-fail --names \"serve,build,queue\" --prefix \"[{name}]\" --prefix-colors \"blue,green,yellow\" \"php artisan serve --port=8000\" \"npm run build\" \"php artisan queue:work database --queue=imports,default --timeout=7200 --memory=1024 --tries=1 --sleep=1\""
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.2",
        "@tailwindcss/vite": "^4.0.0",
        "alpinejs": "^3.4.2",
        "autoprefixer": "^10.4.24",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "postcss": "^8.5.6",
        "prettier": "3.8.3",
        "tailwindcss": "^3.4.19",
        "vite": "^7.0.7"
    },
    "dependencies": {
        "@fortawesome/fontawesome-free": "^7.2.0",
        "chart.js": "^4.5.1"
    }
}
